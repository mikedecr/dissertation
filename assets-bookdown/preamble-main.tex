% !TEX root = ../_book/MGD-thesis.tex

% ----------------------------------------------------
%   Preamble for PDF build of thesis
%   
%   - don't do anything that compromises the HTML version!
% ----------------------------------------------------


% fonts
\usepackage[varqu, scaled = 0.95]{zi4}
\usepackage[lf, mathtabular, minionint]{MinionPro}
\usepackage[scaled = 0.9]{sourcesanspro}

% teletype math using zi4?
\renewcommand{\mathtt}[1]{\text{\texttt{#1}}}


% replace epsilon glyph (a Minion problem...)
\AtBeginDocument{\let\epsilon\varepsilon}

% \usepackage{bm} 
% this fails, too many math alphabets. Workaround in each Rmd equal to this:
% \renewcommand{\bm}[1]{\boldsymbol{\mathbf{#1}}} 
% we just do \boldsymbol{} instead (?)


% --- counter  -----------------------

% \setcounter{secnumdepth}{2}










